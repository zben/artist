.row
  .span16
    %h2 All Artists
.row
  .span16
    -@ind_users.each do |artist|
      =link_to artist do
        .artist_block.shadow
          =image_tag artist.avatar.url(:large)
          %h4=artist.name
