-# %ul{:class=>"tabs",:"data-tabs"=>"tabs"}
-#   %li.active=link_to "Biography", @user
-#   %li=link_to "All Artwork",""
-# 
-# .row
-#   =render 'ind_users/side_nav_public'
-#   .span8
-#     %h3="About #{@user.profile.firstname}"
-#     =raw @user.profile.intro
-#     %h3="Sample artwork by #{@user.profile.firstname}"
-#     =raw @user.profile.intro

.row
  .span16
    .user_sidebar.shadow
      =image_tag @user.avatar.url(:large)

    .user_mainbar.shadow
      %h1=@user.name
      %p 美国麻省波士顿    /    12副原画可售   /    20副限量版可售
      %b 画家介绍
      %p 例如例如 例如 例如 例如 例如 例如 例如 例如 例如 例如 例如 例如 例如 例如 例如 例如 例如 例如 例如 例如 例如例如 例如例如 例如例如 例如例如 例如例如 例如例如 例如例如 例如例如 例如例如 例如例如 例如例如 例如例如 例如例如 例如例如 例如例如 例如例如 例如例如 例如例如 例如例如 例如
.row
  .span16#image_blocks
    -@user.artworks.each do |a|
      =link_to a do
        .image_block
          =image_tag(a.photos.first.photo.url(:medium), width: 260)
          .title=a.title == "" ? "Untitled" : a.title
          .artist
            .name
              .image_footer="Submitted #{time_ago_in_words a.created_at} ago"
              =link_to "  Edit details", edit_artwork_path(a), class: "edit_artwork" if current_user == @user

