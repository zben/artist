<% if @is_bookmarked%> 
$("a.<%=@bookmarkable.id=%>").text("unlike").addClass("hidden");
<% else %>
$("a.<%=@bookmarkable.id%>").text("like");
<% end %>
$("span.counter.<%=@bookmarkable.id%>").html(<%=@bookmarkable.likes_counter%>);
